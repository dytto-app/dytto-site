# SEO static files - serve as-is (identity redirect prevents SPA catch)
/robots.txt   /robots.txt   200
/sitemap.xml  /sitemap.xml  200

# SPA fallback for client-side routing (no force flag = won't override existing files)
/*    /index.html   200
