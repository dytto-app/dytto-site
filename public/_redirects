# SEO static files - identity redirects ensure these match BEFORE the wildcard
# Without these, the SPA fallback catches them and serves index.html
/robots.txt   /robots.txt   200
/sitemap.xml  /sitemap.xml  200

# Static well-known files for agent discovery
/.well-known/*  /.well-known/:splat  200

# SPA fallback for client-side routing
# Note: this would catch static files if not for the explicit rules above
/*    /index.html   200
